# 데이터 (개인정보 — 절대 커밋 금지)
data/
*.csv
*.xlsx
*.db
*.db-wal
*.db-shm
# 단, UI 확인용 샘플 시드는 실제 개인정보가 아니므로 추적한다.
# 이게 없으면 배포 환경에서 /api/state 가 500 을 낸다.
!data/seed-cards.json

# 시크릿
.env
.env.*
!.env.example
credentials*.json

# 파이썬
__pycache__/
*.py[cod]
.venv/
venv/

# Node
node_modules/

# OS/IDE
.DS_Store
Thumbs.db
.idea/
.vscode/

# Playwright 로그인 세션 (절대 커밋 금지)
.auth/
playwright-report/
test-results/

# 메일 발송 자격증명 (절대 커밋 금지)
.env

# 스모크 테스트용 임시 페이로드
.smoke/
public/app.js.bak

# 로컬 검증용 임시 DB
.t/
